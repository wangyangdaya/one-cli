mod cli;
mod client;
mod commands;
mod output;
mod types;

#[tokio::main]
async fn main() {
    if let Err(err) = cli::run().await {
        eprintln!("{err}");
        std::process::exit(1);
    }
}
