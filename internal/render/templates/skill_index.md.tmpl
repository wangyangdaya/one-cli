# Generated Skills

Use this file as a lightweight index. Read the per-group `SKILL.md` before acting.

```bash
{{ .App.Name }} skills list
{{ .App.Name }} skills read <skill>
{{ .App.Name }} skills read <skill> references/command-routing.md
```

| Skill | Commands | Guide |
| --- | ---: | --- |
{{- if and .RuntimeBundle (eq .RuntimeBundle.OAuth2GrantType "authorization_code") }}
| `cli-auth` | 3 | [CLI login and session management](cli-auth/SKILL.md) |
{{- end }}
{{- range .App.Groups }}
| `{{ skillName . }}` | {{ len .Operations }} | [`SKILL.md`]({{ skillName . }}/SKILL.md) |
{{- end }}
