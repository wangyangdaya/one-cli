# 生产检查清单：{{ .Group.Name }}

本清单用于把生成的 API 脚手架变成业务可用的 Skill。

## 产品 / 业务

- [ ] `SKILL.md` 的 description 写清了真实用户意图、同义词和边界。
- [ ] 核心概念解释了业务实体、ID、URL、状态、归属和权限。
- [ ] `command-routing.md` 覆盖了常见用户说法和需要避免的命令。
- [ ] `workflows.md` 覆盖了最常见的 3-5 个业务任务。
- [ ] 超出范围请求的跨 Skill 路由已文档化。
- [ ] 面向用户的输出预期已文档化：字段、URL、状态和下一步。

## 工程

- [ ] `{{ .App.Name }} {{ .Group.Name }} --help` 可用。
{{- range .Group.Operations }}
- [ ] `{{ $.App.Name }} {{ $.Group.Name }} {{ .CommandName }} --help` 可用。
{{- end }}
- [ ] `SKILL.md` 中的必填参数、请求体字段和 header 规则与 CLI 一致。
- [ ] JSON 请求体统一记录 `--data` 用法：内联 JSON、`@文件` 或 `-` 标准输入。
- [ ] 只有声明二进制上传字段的操作才记录 `--file`；多文件时记录 `[字段=]路径`。
- [ ] multipart 操作明确哪些文本字段放入 `--data`，哪些二进制字段使用 `--file`。
- [ ] 不要同时让 `--data -` 和 `--file -` 读取标准输入；一次只能消费一个 stdin 流。
- [ ] 认证、环境变量和 base URL 设置准确。

## 安全

- [ ] 写入、删除、批量、清空、重置、导入和不可逆操作需要确认，或有明确例外说明。
- [ ] 缺少 ID 时通过 list/search/get 解析，而不是猜测。
- [ ] 模糊搜索结果需要用户选择。
- [ ] API 支持时，工作流包含回读验证。
- [ ] 权限和租户边界已文档化。

## QA

- [ ] Front matter 有效，Skill 能加载。
- [ ] 文档中的每个命令都真实存在。
- [ ] 至少一个只读示例能成功运行。
- [ ] 至少一个写入工作流已 dry-run，或已在安全环境测试。
- [ ] 空结果、参数非法、未找到和权限拒绝路径有恢复指导。
- [ ] 已知限制在交接前列出。

## 维护

- [ ] API 或 MCP schema 变更会触发重新生成和 review。
- [ ] CLI 命令重命名会更新 `SKILL.md` 和 `references/`。
- [ ] 业务规则变更会更新 workflow 和 routing 文档。
- [ ] PR 或发布说明中记录了验证证据。
