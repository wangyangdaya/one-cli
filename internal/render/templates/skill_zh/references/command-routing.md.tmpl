# 命令路由：{{ .Group.Name }}

用本文件教会 Agent 如何把用户语言映射到真实 CLI 命令。

生成的 API 描述可以列出命令，但不知道“我的待办”“关闭这个单据”“清空当前记录”等业务说法对应哪个命令。生产使用前，请补全 MANUAL 部分。

## 生成命令

| 用户意图 | 推荐命令 | 风险 | 避免 | 备注 |
| --- | --- | --- | --- | --- |
{{- range .Group.Operations }}
| <!-- MANUAL: 添加 {{ .CommandName }} 的常见用户说法 --> | `{{ $.App.Name }} {{ $.Group.Name }} {{ .CommandName }}` | {{ operationRiskLabel . $.SkillLang }} | <!-- MANUAL: 如有，填写容易混淆的错误命令 --> | {{ if .Summary }}{{ .Summary }}{{ else }}{{ .CommandName }}{{ end }} |
{{- end }}

## 选择规则

<!-- MANUAL: 添加相似命令之间的选择规则。示例：
- 用户只给出范围或负责人，没有关键词时，用 `list`。
- 用户给出明确关键词、名称、编码或短语时，用 `search`。
- 只有在有稳定 ID、URL 或唯一查询结果时，才用 `get`。
- `update`、`delete` 或状态变更命令必须先确认目标。
-->

## 术语与同义词

| 用户说法 | 含义 | 命令提示 |
| --- | --- | --- |
| <!-- MANUAL: 业务术语 --> | <!-- MANUAL: 标准含义 --> | <!-- MANUAL: 命令或规则 --> |

## 跨 Skill 边界

| 用户请求 | 使用本 Skill？ | 路由到 | 原因 |
| --- | --- | --- | --- |
| <!-- MANUAL: 超出范围的意图 --> | 否 | <!-- MANUAL: 其他 Skill 或流程 --> | <!-- MANUAL: 原因 --> |

## 信息缺失规则

<!-- MANUAL: 定义缺少必填信息时如何询问用户。示例：
- 没有 ID 时，先 list 或 search，不要编造。
- 找到多个匹配项时，请用户选择。
- 操作会修改数据时，执行前展示目标和动作。
-->

## 请求输入规则

- 内联 JSON 使用 `--data '<json>'`，JSON 文件使用 `--data @path.json`，标准输入使用 `--data -`。
- 只有声明二进制上传字段的命令才使用 `--file path`；多个文件字段使用 `--file field=path`。
- multipart 命令中，文本表单字段放入 `--data`，二进制内容通过 `--file` 传入。
- 不要同时使用 `--data -` 和 `--file -`，stdin 只能消费一次。
