# Generation Report: {{ .Group.Name }}

Generated by `opencli` from the API/MCP input. Use this file to decide what should be fixed upstream and what should be completed manually in the skill package.

## Generated From

- Skill: `{{ .Group.Name }}`
- App: `{{ .App.Name }}`
- Backend: {{ if groupUsesMCPHTTP .Group }}MCP HTTP{{ else if groupUsesMCPStdio .Group }}MCP Stdio{{ else }}HTTP API{{ end }}
- Commands: {{ len .Group.Operations }}

## Detected Gaps

{{- $issues := groupDocumentationIssues .Group .SkillLang }}
{{- if $issues }}
{{- range $issues }}
- [ ] {{ . }}
{{- end }}
{{- else }}
- No obvious documentation gaps detected.
{{- end }}

## Manual Follow-Up

API descriptions usually cannot provide these business details:

1. User intents, synonyms, and boundaries.
2. Meaning of enum-like fields such as `status`, `type`, and `state`.
3. Which fields come from user input and which come from previous command output.
4. Multi-command workflow order and data handoff.
5. Permission, tenant, and state checks.
6. Common error recovery guidance.
7. Time, pagination, sorting, and filtering conventions.

