# {{ .Group.Name }} Skill 包

由 `opencli` 为 `{{ .App.Name }} {{ .Group.Name }}` 自动生成。

本目录需要交给产品、业务、工程和 QA 共同完善。已生成的文件包含从 API/MCP 描述中提取的命令、参数和基础工作流；真实的业务规则、枚举含义和跨命令流程需要人工补充。

## 目录结构

```text
skills/{{ groupPackageName .Group }}/
├── SKILL.md                  # Agent 入口文件（最重要）
├── README.md                 # 本文件：说明目录结构和协作流程
├── assets/
│   └── demo-request.json     # 用于 --file 示例的占位请求体
├── references/
│   ├── command-routing.md    # 用户意图 → CLI 命令映射
│   ├── workflows.md          # 多步骤业务流程
│   └── production-checklist.md # 生产上线检查清单
└── GENERATION_REPORT.md      # 生成时检测到的 Swagger/输入缺陷
```

## 文件用途

| 文件 | 责任人 | 用途 |
|---|---|---|
| `SKILL.md` | Skill 维护者 | Agent 入口。包含触发规则、安全约束、命令索引、示例和短工作流。 |
| `README.md` | 所有人 | 说明本包结构和交接流程。 |
| `assets/demo-request.json` | 工程 + QA | 支持 `--file` 的命令的占位 JSON 请求体，可作为示例和手工编辑起点。 |
| `references/command-routing.md` | 产品 + Skill 维护者 | 把用户自然语言映射到正确命令。 |
| `references/workflows.md` | 产品 + 工程 + QA | 定义真实的跨命令业务流程。 |
| `references/production-checklist.md` | QA + 维护者 | 上线前的验收检查清单。 |
| `GENERATION_REPORT.md` | 生成工具 | 记录生成时发现的 Swagger 缺陷，便于定向补充。 |

## 哪些文件会被重新生成覆盖

| 文件 | 是否会被覆盖 | 说明 |
|---|---|---|
| `SKILL.md` | 是 | 可在业务规则稳定后逐步迁移到 `references/`；Swagger 更新后需合并人工补充内容。 |
| `assets/demo-request.json` | 是 | 每次生成会根据请求体 schema 更新。 |
| `GENERATION_REPORT.md` | 是 | 每次生成重新评估。 |
| `references/workflows.md` | 否 | 人工编写，不会被覆盖。 |
| `references/command-routing.md` | 否 | 人工编写，不会被覆盖。 |
| `references/production-checklist.md` | 否 | 人工编写，不会被覆盖。 |

## 完成顺序

1. 运行 `{{ .App.Name }} {{ .Group.Name }} --help` 及每个子命令 `--help`，确认命令表面。
2. 阅读 `GENERATION_REPORT.md`，补充 Swagger 中缺失的描述、枚举和分组信息。
3. 用 `assets/demo-request.json` 作为起点，构造真实的 `--file` 示例。
4. 填写 `references/command-routing.md`，列出常见用户请求和应调用的命令。
5. 填写 `references/workflows.md`，补充 top 3-5 业务工作流。
6. 把最重要的路由和工作流摘要写回 `SKILL.md`。
7. 用 `references/production-checklist.md` 验收。

## 已生成命令列表

| 命令 | 说明 | 风险等级 |
|---|---|---|
{{- range .Group.Operations }}
| `{{ $.App.Name }} {{ $.Group.Name }} {{ .CommandName }}` | {{ if .Summary }}{{ .Summary }}{{ else }}{{ generateUserIntentChinese . }}{{ end }} | {{ operationRiskLevelChinese (operationRiskLevel .) }} |
{{- end }}

## 不可忽视

- 替换或完善 `SKILL.md` 中的 description，使其描述真实用户意图和边界。
- 不要仅根据 API 名称臆造业务流程。
- 不要移除写入、删除、批量、不可逆操作的安全检查。
- 长示例、详细 schema 和恢复说明放在 `references/`；`SKILL.md` 保持精简，便于 Agent 加载。
