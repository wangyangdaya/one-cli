---
name: cli-auth
version: 1.0.0
description: "{{ .App.Name }} 的 CLI 登录与会话管理。调用受保护业务接口前、出现 login_required 或需要退出登录时使用。"
metadata:
  requires:
    bins: ["{{ .App.Name }}"]
  cliHelp: "{{ .App.Name }} login --help"
---

# CLI 登录与会话管理

这是 CLI 内置认证命令，不是业务 API 的 `auth` 命令组。

## 路由规则

- 调用受保护业务命令前若没有登录态，执行 `{{ .App.Name }} login`。
- 无浏览器或远程终端环境执行 `{{ .App.Name }} login --no-browser`，把输出的授权链接交给用户打开并确认。
- 命令返回 `login_required` 时，执行一次登录后重试原业务命令。
- 使用 `{{ .App.Name }} status` 离线查看本地会话状态；业务命令会自动刷新 `needs_refresh` 会话。
- 收到 HTTP 401 时可以先检查登录态；重新登录后最多重试一次，避免循环登录。
- `{{ .App.Name }} logout` 会删除本地业务 Token，只能在用户明确要求退出或清除会话时执行。
- 不要使用业务命令 `{{ .App.Name }} auth login` 代替 CLI 登录；它属于 OpenAPI 业务接口。

## 命令

```bash
{{ .App.Name }} login
{{ .App.Name }} login --no-browser
{{ .App.Name }} status
{{ .App.Name }} logout
```

CLI 登录会打开 IAM 授权页、接收授权码、向业务系统登录接口换取业务 Token，并在后续受保护请求中自动注入该 Token。账号密码只在 IAM 页面输入，不写入 CLI 配置或 Skill。
