# {{ .App.Name }}

由 `opencli` 自动生成。

## 版本

生成的 Go CLI 使用 `{{ goAppVersion .App }}` 作为 `--version` 返回值。可通过 `opencli generate --app-version` 或 `opencli.yaml` 中的 `app.version` 设置。

```bash
./bin/{{ .App.Name }} --version
```

{{- if appHasMCPHTTP .App }}
## MCP Transport

本 CLI 包含从 `streamable_http` MCP 服务器生成的命令。

- 生成时通过 `initialize` 和 `tools/list` 发现工具
- 运行时通过 `tools/call` 执行工具
- 可通过 `OPENCLI_BASE_URL` 覆盖生成时的 endpoint

```bash
export OPENCLI_BASE_URL='http://127.0.0.1:8000/mcp'
```
{{- end }}

{{- if appHasMCPStdio .App }}
## MCP Stdio

本 CLI 包含从 `stdio` MCP 服务器生成的命令。

- 生成时通过 `initialize` 和 `tools/list` 发现工具
- 运行时启动配置的 MCP 进程并发送 `tools/call`
{{- end }}

{{- if not (appHasAnyMCP .App) }}
## 环境配置

设置目标 API 基础地址：

```bash
export OPENCLI_BASE_URL='http://127.0.0.1:8000'
```
{{- end }}

## 请求体输入

简单 JSON 请求体端点支持顶层参数和原始 JSON 输入模式：

```bash
./bin/{{ .App.Name }} auth login --email you@example.com --password secret --remember
./bin/{{ .App.Name }} auth login --data '{"email":"you@example.com","password":"secret","remember":true}'
./bin/{{ .App.Name }} auth login --file login.json
```

{{- if appHasAnyMCP .App }}
## MCP 示例

{{- range .App.Groups }}
{{- $group := . }}
{{- range .Operations }}
{{- if eq .BodyMode "simple-json" }}
```bash
./bin/{{ $.App.Name }} {{ $group.Name }} {{ .CommandName }}{{ range .BodyFields }}{{ if .Required }} --{{ .Name }} "{{ exampleValue .Type .Name }}"{{ end }}{{ end }}
```
{{- else if eq .BodyMode "file-or-data" }}
```bash
./bin/{{ $.App.Name }} {{ $group.Name }} {{ .CommandName }} --data '{"field":"value"}'
```
{{- end }}
{{- end }}
{{- end }}
{{- end }}

{{- if not (appHasAnyMCP .App) }}
## 请求头输入

带有 OpenAPI header 参数的端点支持可重复的 `--header` 参数：

```bash
./bin/{{ .App.Name }} auth me --header "Authorization: Bearer token"
```

## 跟踪调试

启用 HTTP 请求/响应跟踪：

```bash
./bin/{{ .App.Name }} --trace auth login --email you@example.com --password secret --remember
```
{{- end }}

## 命令列表

{{- range .App.Groups }}
- `{{ $.App.Name }} {{ .Name }}`
{{- end }}

## 生成的 Skills

每个命令组都包含一个标准化的 Skill 包，位于 `skills/<group>/`：

```text
skills/<group>/
├── SKILL.md                  # Agent 入口
├── README.md                 # Skill 包说明
├── assets/
│   └── demo-request.json     # --file 示例请求体
├── references/
│   ├── command-routing.md    # 用户意图 → 命令映射
│   ├── workflows.md          # 多步骤业务流程
│   └── production-checklist.md # 上线检查清单
└── GENERATION_REPORT.md      # 生成缺陷报告
```

生成的 Skill 是 API 脚手架。`assets/demo-request.json` 是合法的占位 JSON，可直接用于文档中的 `--file` 示例；在投入生产前，请用真实的业务意图、路由规则、工作流、安全约束和验证证据填充 `SKILL.md` 和 `references/` 中的 `MANUAL` 部分。
