mod cli;
mod client;
mod commands;
mod output;
mod trace;
mod types;

#[tokio::main]
async fn main() {
    if let Err(err) = cli::run().await {
        output::print_error(env!("CARGO_PKG_NAME"), &err);
        std::process::exit(1);
    }
}
