#!/usr/bin/env sh
set -eu

DIR=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
if [ -z "${OPENCLI_CONFIG:-}" ] && [ -f "$DIR/config/runtime.yaml" ]; then
  OPENCLI_CONFIG="$DIR/config/runtime.yaml"
  export OPENCLI_CONFIG
fi
exec cargo run --manifest-path "$DIR/Cargo.toml" -- "$@"
