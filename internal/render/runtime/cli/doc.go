// Package cli provides command and output helpers copied into generated Go projects.
// The generator's own command wiring lives in its internal/app package.
// Its JSON envelope contract intentionally mirrors internal/output while remaining
// self-contained so generated projects do not depend on the generator module.
package cli
